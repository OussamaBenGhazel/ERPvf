{% extends 'base.html.twig' %}

{% block title %}Fournisseur{% endblock %}

{% block body %}
    <h1>Fournisseur</h1>

    <div class="table-container">
        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ fournisseur.id }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ fournisseur.nom }}</td>
                </tr>
                <tr>
                    <th>Adresse</th>
                    <td>{{ fournisseur.adresse }}</td>
                </tr>
                <tr>
                    <th>Numdetel</th>
                    <td>{{ fournisseur.numdetel }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ fournisseur.email }}</td>
                </tr>
                  <tr>
                    <th>Societe</th>
                    <td>{{ fournisseur.societe }}</td>
                </tr>
            </tbody>
        </table>
    </div>

    <div class="actions">
        <a href="{{ path('app_fournisseur_index') }}" class="back-link">Back to list</a>
        <a href="{{ path('app_fournisseur_edit', {'id': fournisseur.id}) }}" class="btn">Edit</a>
        {{ include('fournisseur/_delete_form.html.twig') }}
    </div>

  
{% endblock %}

